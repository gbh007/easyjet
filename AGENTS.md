# Информация для ИИ агентов

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**ВСЕГДА** следуй правилам в `docs/go/rules.md` — это обязательное требование.

**НИКОГДА** не запускай команды напрямую в обход `task`.

## Документация проекта

Документация располагается в `docs/`:

- `docs/go/` — документация по Go
  - [Архитектура кода](docs/go/arch.md)
  - [Список библиотек](docs/go/libs.md)
  - [Правила написания кода](docs/go/rules.md) ⚠️ **ОБЯЗАТЕЛЬНО К ПРОЧТЕНИЮ**

## Обязательные действия после модификации кода

После **любого** изменения Go кода:

1. Прочитай `docs/go/rules.md` если забыл правила
2. Выполни **все** команды через `task` как указано в `docs/go/rules.md`
3. Убедись что все команды прошли успешно

## Запрещено

- ❌ Изменять код без последующего выполнения `task go:format`, `task go:lint`, `task go:test`, `task build:server`
- ❌ Запускать команды напрямую вместо `task`
- ❌ Игнорировать ошибки линтера или сборки
